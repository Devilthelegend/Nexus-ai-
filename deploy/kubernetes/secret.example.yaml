# Example only. Do NOT commit real secrets. Create the real Secret out-of-band,
# e.g. `kubectl create secret generic nexus-secrets --from-env-file=.env.prod`,
# or manage it via a sealed-secrets / external-secrets operator.
apiVersion: v1
kind: Secret
metadata:
  name: nexus-secrets
  namespace: nexus
  labels:
    app.kubernetes.io/name: nexusai
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://nexus:CHANGE_ME@nexus-postgres:5432/nexus"
  JWT_SECRET_KEY: "CHANGE_ME_TO_A_LONG_RANDOM_VALUE_AT_LEAST_32_BYTES"
  LLM_API_KEY: ""
  SENTRY_DSN: ""
